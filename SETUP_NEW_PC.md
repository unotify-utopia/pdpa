# คู่มือการตั้งค่าคอมพิวเตอร์เครื่องใหม่ & การสลับทำงาน 2 เครื่อง (Seamless Cross-Machine Workflow)

เอกสารนี้จัดทำขึ้นสำหรับช่วยให้คุณสามารถ **สลับการทำงานระหว่างคอมพิวเตอร์ 2 เครื่อง (หรือมากกว่า)** ได้อย่างง่ายดาย โดยไม่ต้องพิมพ์คำสั่ง Git ที่ซับซ้อนด้วยตัวเอง เพราะเราได้สร้าง **"3 ปุ่มวิเศษ (ไฟล์ .bat สำหรับดับเบิลคลิก)"** ไว้ให้เรียบร้อยแล้วครับ!

---

## 🚀 1. การเตรียมความพร้อมบน "คอมพิวเตอร์เครื่องใหม่" (ทำครั้งแรกครั้งเดียว)

### 1.1 ติดตั้งโปรแกรมพื้นฐาน
1. **Node.js**: แนะนำให้ดาวน์โหลดและติดตั้งเวอร์ชัน **LTS (20.x หรือ 22.x)** จาก https://nodejs.org
2. **Git**: ดาวน์โหลดและติดตั้งจาก https://git-scm.com

### 1.2 ดาวน์โหลดโปรเจกต์ (Clone Repository)
เปิดโปรแกรม **Terminal (PowerShell หรือ CMD)** ในโฟลเดอร์ที่คุณต้องการจัดเก็บโปรเจกต์ แล้วรันคำสั่ง:
```bash
git clone https://github.com/unotify-utopia/pdpa.git
cd pdpa
```

### 1.3 ติดตั้งแพ็กเกจที่จำเป็นทั้งหมด (Dependencies)
ในโฟลเดอร์ `pdpa` ให้รันคำสั่ง:
```bash
npm install
npm --prefix super-admin-app install
```

### 1.4 คัดลอกไฟล์ `.env` (สำคัญมาก ⚠️)
เนื่องจากไฟล์ `.env` เก็บข้อมูลลับ (รหัสผ่านฐานข้อมูล, API Key, รหัสผ่านอีเมล) จึง**ไม่ถูกบันทึกขึ้น GitHub** เพื่อความปลอดภัย:
- ให้คุณ **คัดลอกไฟล์ `.env` จากคอมพิวเตอร์เครื่องแรกมาวางไว้ในโฟลเดอร์โปรเจกต์บนคอมพิวเตอร์เครื่องใหม่**
- *(หรือสามารถคัดลอกไฟล์ `.env.example` แล้วเปลี่ยนชื่อเป็น `.env` พร้อมกรอกค่ารหัสผ่านจริง)*

---

## 🪄 2. ระบบ "3 ปุ่มวิเศษ" สำหรับการทำงานประจำวัน (ไม่ต้องพิมพ์คำสั่งเอง)

เราได้สร้างไฟล์ `.bat` สำหรับคลิกใช้งานอัตโนมัติบน Windows ไว้ในโฟลเดอร์โปรเจกต์แล้ว คุณสามารถใช้งานตามขั้นตอนง่าย ๆ ดังนี้ครับ:

### 🟩 ปุ่มที่ 1: `1_เริ่มงาน_ดึงโค้ดล่าสุด.bat`
- **ใช้ตอนไหน**: **ก่อนเริ่มทำงานทุกครั้ง** ไม่ว่าจะนั่งทำงานอยู่คอมพิวเตอร์เครื่องใดก็ตาม
- **ทำอะไรให้บ้าง**: ระบบจะไปเช็ค GitHub และดึงโค้ดเวอร์ชันล่าสุดมาใส่เครื่องให้ทันที (`git pull origin main`) พร้อมติดตั้งแพ็กเกจใหม่ (ถ้ามี)

### 🟦 ปุ่มที่ 2: `2_เลิกงาน_บันทึกและส่งโค้ด.bat`
- **ใช้ตอนไหน**: **ก่อนเลิกงาน หรือก่อนสลับไปทำงานบนคอมอีกเครื่อง**
- **ทำอะไรให้บ้าง**: ระบบจะรวบรวมไฟล์ที่คุณแก้ไขทั้งหมด บันทึก (`git commit`) และส่งขึ้น GitHub (`git push origin main`) ให้อัตโนมัติ โดยคุณสามารถพิมพ์ข้อความสรุปสั้น ๆ ก่อนส่งได้ทันที

### 🟧 ปุ่มที่ 3: `3_อัปเดตขึ้น_Production_Server.bat`
- **ใช้ตอนไหน**: **เมื่อต้องการอัปเดตโค้ดขึ้น Production Server จริง (`119.59.124.169`)**
- **ทำอะไรให้บ้าง**: ระบบจะเชื่อมต่อ SSH ไปยัง Production Server รันดึงโค้ดล่าสุด คอมไพล์ระบบ (`npm run build`) และสั่งรีสตาร์ท Service (`pm2 restart pdpa-backend`) ให้แบบอัตโนมัติ 100%!

---

## 📋 แผนผังสรุปการสลับเครื่อง (Work Flow)

```
[คอมเครื่องที่ 1]
 └── 🖱️ ดับเบิลคลิก "1_เริ่มงาน_ดึงโค้ดล่าสุด.bat" ➔ ทำงาน... ➔ 🖱️ ดับเบิลคลิก "2_เลิกงาน_บันทึกและส่งโค้ด.bat"
                                                                                 │
                                                                   ส่งโค้ดขึ้น GitHub สำเร็จ
                                                                                 │
                                                                                 ▼
[คอมเครื่องที่ 2]                                                       (GitHub Repository)
 └── 🖱️ ดับเบิลคลิก "1_เริ่มงาน_ดึงโค้ดล่าสุด.bat" ◄── ดึงโค้ดล่าสุดมาทำงานต่อทันที! ────┘
```

---

## 💡 คำแนะนำเพื่อความปลอดภัยไร้รอยต่อ
1. **อย่าลืมกดปุ่มที่ 2 (`2_เลิกงาน_บันทึกและส่งโค้ด.bat`) ก่อนปิดคอมเสมอ** เพื่อให้คอมอีกเครื่องมีโค้ดล่าสุดไปทำงานต่อได้
2. **หากมีการตั้งค่า API Keys หรือรหัสผ่านใหม่ใน `.env`** ต้องไม่ลืมนำไปอัปเดตใส่ไฟล์ `.env` ในคอมอีกเครื่องและบน Production Server ด้วยครับ

---

## 🗂️ โครงสร้างไฟล์หลัก (อัปเดต 2026-08 - v2.0 Modular Architecture)

```
routes/
  auth.routes.js       ← Login, 2FA
  users.routes.js      ← User CRUD
  workflow.routes.js   ← Workflow Engine
  reports.routes.js    ← DPO Analytics
  requests.routes.js   ← Request management
  download.routes.js   ← Secure download
  superadmin.routes.js ← Tenant & SuperAdmin
  public.routes.js     ← Public APIs
migrations/
  001_workflow_engine.sql      ← รัน UP เพื่อสร้างตาราง
  001_workflow_engine_down.sql ← รัน DOWN เพื่อ rollback
```

## 🔧 การรัน DB Migration บน Production (สำหรับโครงสร้างใหม่)
หากระบบมีการเพิ่มตารางใหม่ (เช่น workflow_states, workflow_transitions) จำเป็นต้องรัน Migration SQL เข้า Database โดยรันคำสั่ง:
```bash
psql -U pdpa_admin -d pdpa_prod_db -f migrations/001_workflow_engine.sql
```
